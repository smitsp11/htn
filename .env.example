# The supplied Federato integration API. This is part of the core challenge,
# not an optional enrichment API.
FEDERATO_API_URL=https://product.federato.ai/integrations-api/handlers/federato-hack-north?outputOnly=true
FEDERATO_AUTH_URL=https://auth.product.federato.ai/oauth/token
FEDERATO_CLIENT_ID=
FEDERATO_CLIENT_SECRET=

# The PDFs name the correct Auth0 domain but do not print the audience value.
# Copy the audience from Federato's API Documentation/organizer credentials.
FEDERATO_AUDIENCE=

# Data source (tri-state):
#   unset  -> offline real snapshot from raw/full_*.json (DEFAULT; the real 158
#             submissions captured from Federato, evaluated through the same
#             schema-planner + adapter path as live)
#   true   -> synthetic demo fixtures in lib/demo/submissions.ts
#   false  -> live Auth0 + Federato API (requires FEDERATO_AUDIENCE and the exact
#             Query Request Body)
# FEDERATO_USE_DEMO_DATA=

# Queries are compiled from the discovered schema at runtime by the query agent
# (lib/federato/adapter.ts); there is no hand-written query body to pin.

# Optional planner model (OFF by default). When FEDERATO_PLANNER_PROVIDER names
# a provider whose key is set, the agent asks the model which discovered field
# answers each appetite requirement; every path it picks is re-resolved against
# the schema before use, and the query itself is still compiled deterministically.
# Leave unset for deterministic schema name-matching only.
#   FEDERATO_PLANNER_PROVIDER=openai | anthropic
#   FEDERATO_PLANNER_MODEL=<model id>  (defaults: gpt-5.6-sol / claude-opus-5)
FEDERATO_PLANNER_PROVIDER=
FEDERATO_PLANNER_MODEL=
ANTHROPIC_API_KEY=

# OpenAI — powers the ask-the-queue natural-language layer (server-side only).
# Put the real key in .env.local, never here. The queue is scored deterministically;
# the model only interprets the question and phrases a grounded answer.
OPENAI_API_KEY=
# Optional model override (default: gpt-4.1)
OPENAI_MODEL=

# Optional Browserbase — live "Run consolidation" demo (W8). When unset, the
# button falls back to fixture channels with the same values.
BROWSERBASE_API_KEY=
BROWSERBASE_PROJECT_ID=
