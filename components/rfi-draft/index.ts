// The stylesheet is loaded once via `@import` in app/globals.css so this
// component tree stays importable under node tests.
export { RfiDraft } from "./rfi-draft";
export type { RfiDraftProps } from "./rfi-draft";
