"use client";

import type { RankedSubmission } from "@/lib/domain/types";

export function DecisionSidebar({ submission }: { submission: RankedSubmission }) {
  return <aside className="decision-sidebar" data-stub />;
}
